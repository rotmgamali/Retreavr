# Database (Supabase PostgreSQL)
POSTGRES_HOST=your-project-ref.supabase.co
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# JWT Auth
JWT_SECRET=generate-a-secure-key-here

# CORS (not needed for same-origin Next.js API routes)
